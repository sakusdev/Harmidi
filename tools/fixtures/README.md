# Note-evaluation fixtures

`reference.json` contains ground-truth note events and `prediction.json` contains a near-perfect prediction used by the evaluator self-test.

These fixtures validate metric calculation only. Real transcription quality must be evaluated with a larger corpus containing audio, aligned reference MIDI, and per-category metadata as described in `docs/reliability.md`.
